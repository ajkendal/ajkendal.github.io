import styled from 'styled-components'

/* 
  linkOne: string
  linkOneText: string
  linkTwo: string
  linkTwoText: string
*/

const BottomLinks = (props) => {
  return <BottomLinksDiv></BottomLinksDiv>
}

const BottomLinksDiv = styled.div``

export default BottomLinks
